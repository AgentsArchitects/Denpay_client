# API Configuration
# Vite automatically loads the correct file based on the command:
#   npm run dev   → uses .env.development (http://localhost:8000/api)
#   npm run build → uses .env.production  (Azure URL)
#
# To override, create a .env.local file (git-ignored):
# VITE_API_BASE_URL=http://localhost:8000/api
